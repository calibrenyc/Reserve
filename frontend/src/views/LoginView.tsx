import React, { useState } from 'react';
import reserveLogo from '../assets/reserve-logo.png';
export const LoginView: React.FC<{onLogin: (session: any) => void}> = ({onLogin}) => {
  const [email, setEmail] = useState('owner@reserve.local'); const [password, setPassword] = useState('ChangeMeNow!'); const [error, setError] = useState('');
  const submit = async (e: React.FormEvent) => { e.preventDefault(); setError(''); const res = await fetch('/api/auth/login', {method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({email,password})}); if (!res.ok) return setError('Unable to sign in. Check your email and password.'); const data = await res.json(); localStorage.setItem('reserve_token', data.access_token); localStorage.setItem('reserve_location_id', data.user.locations[0]?.id || ''); onLogin(data.user); };
  return <main className="min-h-screen bg-black flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-md bg-zinc-900 border border-zinc-700 rounded-2xl p-8 space-y-5 shadow-2xl"><div><img src={reserveLogo} alt="Reserve" className="w-64 max-w-full h-auto" /><p className="text-zinc-400 mt-4">Sign in to your organization.</p></div><label className="block text-sm">Email<input className="mt-1 w-full p-3 rounded bg-zinc-800 border border-zinc-700" value={email} onChange={e=>setEmail(e.target.value)} /></label><label className="block text-sm">Password<input type="password" className="mt-1 w-full p-3 rounded bg-zinc-800 border border-zinc-700" value={password} onChange={e=>setPassword(e.target.value)} /></label>{error && <p className="text-red-400 text-sm">{error}</p>}<button className="w-full p-3 rounded bg-emerald-600 font-semibold">Sign in</button><p className="text-xs text-zinc-500">Demo accounts: owner@reserve.local or manager@reserve.local · ChangeMeNow!</p></form></main>;
};
